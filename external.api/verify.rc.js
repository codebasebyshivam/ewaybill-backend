import dotenv from 'dotenv';
dotenv.config();
import queryDatabase from '../services/sql.query.executor.js';
import axios from 'axios';

const fetchRC = async (vehicle_number, username, db_name) => {
  try {
  } catch (error) {}
};

export default fetchRC;
